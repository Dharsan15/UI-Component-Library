import React from "react";

function AlertModal() {
  return (
    <div>
      <div>Are you sure you Delete ?</div>

      <div>
        <button>Delete</button>
        <button>Cancel</button>
      </div>
    </div>
  );
}

export default AlertModal;
